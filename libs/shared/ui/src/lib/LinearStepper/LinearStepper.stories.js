import LinearStepper from './LinearStepper';

export default {
  component: LinearStepper,
  title: 'LinearStepper',
};

const Template = (args) => <LinearStepper {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
