import ButtonQuantity from './ButtonQuantity';

export default {
  component: ButtonQuantity,
  title: 'ButtonQuantity',
};

const Template = (args) => <ButtonQuantity {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
